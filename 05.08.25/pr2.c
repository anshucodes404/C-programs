// Given an unsorted array of size n, WAP using pointer and DMA to find and display the number of
// elements between two elements a and b(both inclusive).

#include<stdio.h>

    int main(){
        int n, a, b;
        printf("Enter the size of array: ");
        scanf("%d", &n);
        

}